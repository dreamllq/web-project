/**
 * Request Verification DTO
 *
 * Empty DTO for the POST /api/v1/auth/verify-email/request endpoint.
 * User information is obtained from the JWT token via @CurrentUser decorator.
 */
export class RequestVerificationDto {}
